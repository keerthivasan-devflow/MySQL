CREATE TABLE Transactions (
    transaction_id INT AUTO_INCREMENT PRIMARY KEY,
    account_id INT NOT NULL,
    transaction_type enum("Withdraw", "Deposit") NOT NULL,
    amount DECIMAL(15, 2) NOT NULL,
    transaction_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    description VARCHAR(255),
    FOREIGN KEY (account_id) REFERENCES Accounts(account_id) ON DELETE CASCADE
);

CREATE TABLE Accounts (
    account_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_id INT NOT NULL,
    account_type ENUM("Savings", "Current") NOT NULL,
    balance DECIMAL(15, 2) DEFAULT 0.00,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (customer_id) REFERENCES Customers(customer_id) ON DELETE CASCADE
);

CREATE TABLE Customers (
    customer_id INT AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    phone_number VARCHAR(15),
    date_of_birth DATE,
    address VARCHAR(255)
);

INSERT INTO Transactions (account_id, transaction_type, amount, description)
VALUES
    (1, 'Deposit', 500.00, 'Initial deposit'),
    (2, 'Withdraw', 200.00, 'ATM withdraw'),
    (3, 'Deposit', 1500.00, 'Direct deposit'),
    (4, 'Withdraw', 300.00, 'Cash withdraw'),
    (5, 'Deposit', 800.00, 'Salary deposit'),
    (6, 'Withdraw', 1500.00, 'Bill payment'),
    (7, 'Deposit', 1000.00, 'Check deposit'),
    (8, 'Withdraw', 120.00, 'ATM withdraw'),
    (9, 'Deposit', 500.00, 'Transfer from savings'),
    (10, 'Withdraw', 1000.00, 'Cash withdraw'),
    (11, 'Deposit', 2000.00, 'Direct deposit'),
    (12, 'Withdraw', 800.00, 'Online purchase'),
    (13, 'Deposit', 2500.00, 'Gift received'),
    (14, 'Withdraw', 1200.00, 'Bill payment'),
    (15, 'Deposit', 3000.00, 'Transfer from external bank');
