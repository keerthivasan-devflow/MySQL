## 1. LEAD(expr, offset, default) | LAG(expr, offset, default)

- expression is required
- offset is an optional - number of rows to be forwarded or backwarded from current row but default is 1
- default is also an optional - If there are no next/previous row then SQL returns NULL
